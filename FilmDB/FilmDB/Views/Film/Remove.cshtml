@model FilmDB.Models.FilmModel

@{
    ViewData["Title"] = "Remove";
}

<h1>Remove</h1>

<div>
    <h4>FilmModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Year)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Year)
        </dd>
    </dl>
</div>
<div>
    <form asp-action="RemoveConfirm" method="post">
        <input hidden asp-for="ID" />
        <input type ="submit" value="Potwierdz usuniecie!" class="btn btn-outline-danger"</input>
        @*<a asp-action="RemoveConfirm" asp-route-id="@Model.ID">Usunięcie</a>*@
    </form>
    @*<a asp-action="RemoveConfirm" asp-route-id="@Model.ID">Usunięcie</a>*@
    <a class="btn btn-outline-warning" asp-action="Index">Strona główna</a>
</div>
